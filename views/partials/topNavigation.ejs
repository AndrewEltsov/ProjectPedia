  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/pedia"><%= blocks.title %></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>  
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <% if (user) { %>
          <a class="nav-item nav-link" href="/pedia">На главную</a>
          <a class="nav-item nav-link" href="/editor">Написать статью</a>  
          <a class="nav-item nav-link" href="/users">Список пользователей</a>      
        <% } %>
        <% if (user) { %>
          <a class="nav-item nav-link" id="logout" href="">Выйти</a> 
          <% } else { %>
            <a class="nav-item nav-link" href="/login">Войти</a>
          <% } %>
      </div>
    </div>
  </nav>

  <script>
  $(document.getElementById('logout')).on('click', function() {
    var form = $(this);
    $.ajax({
        url: "/logout",
        method: "POST",
        success: function (result) {
            window.location.href='/'
        },
        error: function(res){
            alert('error');
        }
      });
      return false; 
});
  </script>